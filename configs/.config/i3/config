# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

###---Basic Settings---###
set $browser    /usr/bin/firefox
set $term       /usr/local/bin/st
set $primdisp   DVI-D-0
set $extdisp    HDMI-0
set $mod        Mod4

###---Visual Settings---###
font pango:DejaVu Sans Mono 8
## gaps
hide_edge_borders smart
#gaps inner 10
#gaps outer 10
#smart_gaps on
default_border pixel 3

# workspace alias
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

###---Rules for apps---###
# use xprop the check window properties
for_window [class="Arandr"]         floating enable
for_window [class="Steam"]          floating enable
for_window [class="Matplotlib"]     floating disable
for_window [class="mqtt-explorer"]     floating enable
assign [class="Matplotlib"]         $ws9

# dropdown terminal
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 625 400
for_window [instance="dropdown"] move position center
for_window [instance="dropdown"] move scratchpad
bindsym $mod+d [instance="dropdown"] scratchpad show; move position center

# dropdown calc
for_window [class="SpeedCrunch"] floating enable
for_window [class="SpeedCrunch"] resize set 625 400
for_window [class="SpeedCrunch"] move position center
for_window [class="SpeedCrunch"] move scratchpad
bindsym $mod+Shift+c [class="SpeedCrunch"] scratchpad show; move position center

# Telegram desktop
for_window [title="TelegramDesktop"] floating enable
for_window [title="TelegramDesktop"] resize set 625 400
for_window [title="TelegramDesktop"] move position center
for_window [title="TelegramDesktop"] move scratchpad
bindsym $mod+c [class="TelegramDesktop"] scratchpad show; move position center

# mqtt-explorer
for_window [class="MQTT-Explorer"] floating enable
for_window [class="MQTT-Explorer"] resize set 1000 800
for_window [class="MQTT-Explorer"] move position center
for_window [class="MQTT-Explorer"] move scratchpad
bindsym $mod+q [class="MQTT-Explorer"] scratchpad show; move position center

bindsym $mod+Shift+Tab workspace back_and_forth

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Shift+Return   exec $term
bindsym $mod+Return         exec $term -e tmux new-session -A -s base
bindsym $mod+m              exec --no-startup-id $HOME/bin/dmenu/mux

# kill focused window
bindsym $mod+Shift+q kill

# start launcher
bindsym $mod+r exec dmenu_run -fn 'DejaVuSansMono Nerd Font Mono-14'
# bindsym $mod+Shift+c exec clipmenu
#bindsym $mod+r exec rofi -show run
#bindsym $mod+Tab exec rofi -show window

###---KeyBind---###
# Change Focus
bindsym $mod+h              focus left
bindsym $mod+j              focus down
bindsym $mod+k              focus up
bindsym $mod+l              focus right

# Move Focused Window
bindsym $mod+Shift+h        move left 40px
bindsym $mod+Shift+j        move down 40px
bindsym $mod+Shift+k        move up 40px
bindsym $mod+Shift+l        move right 40px

# Resize
bindsym $mod+y              resize shrink width 5 px or 5 ppt
bindsym $mod+u              resize grow height 5 px or 5 ppt
bindsym $mod+i              resize shrink height 5 px or 5 ppt
bindsym $mod+o              resize grow width 5 px or 5 ppt
bindsym $mod+Shift+y        resize shrink width 30 px or 30 ppt
bindsym $mod+Shift+u        resize grow height 30 px or 30 ppt
bindsym $mod+Shift+i        resize shrink height 30 px or 30 ppt
bindsym $mod+Shift+o        resize grow width 30 px or 30 ppt

# Toggle split orientation
bindsym $mod+v split toggle

# Fullscreen
bindsym $mod+f fullscreen toggle

# Container layout: tabbed/ split
bindsym $mod+Shift+t        layout toggle split
bindsym $mod+t              layout tabbed

# toggle tiling / floating
bindsym $mod+Shift+space    floating toggle

# change focus between tiling / floating windows
bindsym $mod+space          focus mode_toggle

# Cycle through window
bindsym $mod+bracketright   workspace next
bindsym $mod+bracketleft    workspace prev

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Move to scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show scratchpad
bindsym $mod+minus scratchpad show

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

workspace $ws1 output $primdisp
workspace $ws2 output $primdisp
workspace $ws3 output $primdisp
workspace $ws4 output $primdisp
workspace $ws5 output $primdisp
workspace $ws6 output $primdisp
workspace $ws7 output $extdisp
workspace $ws8 output $extdisp
workspace $ws9 output $extdisp
workspace $ws10 output $extdisp

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+x exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        font pango:mono 10
        status_command i3blocks
        position top
        mode dock
        tray_output $primdisp
}

# BindKey
## Power Management
#http://www.michaelabrahamsen.com/posts/custom-lockscreen-i3lock/
#set $Locker "xautolock -locknow"
#set $Locker "~/bin/lockscreen/lockscreen.sh"
set $Locker "playerctl pause; i3lock-fancy --pixelate -f ..."
set $LockerSusp "systemctl suspend"

## Display
bindsym XF86Display exec --no-startup-id "~/bin/dmenu/display"
bindsym $mod+F5 exec --no-startup-id "~/bin/dmenu/display"

## Touchpad Toggle
bindsym XF86TouchpadToggle exec --no-startup-id "~/bin/toggle_toucpad.sh"

## System command
bindsym Pause   exec --no-startup-id "~/bin/dmenu/systemcmd"
bindsym $mod+F9 exec --no-startup-id "~/bin/dmenu/systemcmd"

## MediaKey
bindsym $mod+Shift+Next         exec pulsemixer --toggle-mute && pkill -RTMIN+10 i3blocks
bindsym $mod+F1                 exec pulsemixer --toggle-mute && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute           exec pulsemixer --toggle-mute && pkill -RTMIN+10 i3blocks

bindsym $mod+Prior              exec pulsemixer --unmute --change-volume +5 && pkill -RTMIN+10 i3blocks
bindsym $mod+F3                 exec pulsemixer --unmute --change-volume +5 && pkill -RTMIN+10 i3blocks
bindsym XF86AudioRaiseVolume    exec pulsemixer --unmute --change-volume +5 && pkill -RTMIN+10 i3blocks

bindsym $mod+Next               exec pulsemixer --unmute --change-volume -5 && pkill -RTMIN+10 i3blocks
bindsym $mod+F2                 exec pulsemixer --unmute --change-volume -5 && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume    exec pulsemixer --unmute --change-volume -5 && pkill -RTMIN+10 i3blocks

bindsym $mod+Up     exec playerctl play-stop
bindsym $mod+Down   exec playerctl play-pause
bindsym $mod+Right  exec playerctl next
bindsym $mod+Left   exec playerctl previous

## printscreen
bindsym Print --release\
            exec scrot -s '%Y-%m-%d_%HH-%MM-%SS_scrot.png' -e 'xclip -selection clipboard -target image/png -i $f && mv $f ~/Pictures/screenshots/'

## Vimwiki
bindsym $mod+n workspace $ws7; \
            exec $term -T "Vimwiki" -e vim "+:VimwikiIndex"

## Ranger
bindsym $mod+e exec $term -T "Ranger" -e ranger
bindsym $mod+Shift+e exec "nautilus"

## Spotify
bindsym $mod+s workspace $ws6; exec spotify;

###---App at startup---###
# Wallpaper
exec_always     feh --bg-fill -z ~/Pictures/wallpaper/
# Dropbox
exec            ~/.dropbox-dist/dropboxd
# Redshift
exec            /usr/bin/redshift
# Udiskie
exec            udiskie
# Compton
exec            --no-startup-id compton --xrender-sync-fence
# Unclutter, hide mouse
exec            --no-startup-id unclutter
# Disable buzzer and switch escape with capslock
exec_always     --no-startup-id xset b off
exec_always     setxkbmap -option caps:swapescape
# Other apps
exec            --no-startup-id $term -n dropdown -e tmux new-session -s dropdown
exec            --no-startup-id speedcrunch
exec            --no-startup-id Telegram
exec            --no-startup-id clipmenud
exec            --no-startup-id nm-applet
exec            --no-startup-id blueman-applet
exec            --no-startup-id xinput set-prop 12 331 1
